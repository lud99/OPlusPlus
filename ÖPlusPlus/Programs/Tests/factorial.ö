global int its = 0;

int fact(int n) => {
	its++;
	if (n <= 1) {
		return 1;
	};

	return n * fact(n - 1);
};

printf("%i\n", fact(1));
printf("%i\n", its);

its = 0;

printf("%i\n", fact(5));
printf("%i\n", its);

its = 0;

printf("%i\n", fact(9));
printf("%i\n", its);